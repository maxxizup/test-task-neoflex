// import { counterSlice } from "@app/providers/store/reducers/BasketSlice.ts";

export const FavouritePage = () => {

    // const {count} = useAppSelector(state => state.counterReducer);
    // const {increment} = counterSlice.actions;

    // console.log(count)

    return (
        <div>
            {/*<h1>{count}</h1>*/}
            {/*Счетчик равен {count}, угар. <button onClick={() => dispatch(increment(10))}>Увеличь</button>*/}
        </div>
    )
}

export default FavouritePage;
